{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); CMD=$(echo \"$INPUT\" | jq -r \".tool_input.command // empty\"); if [[ \"$CMD\" == git\\ commit* ]]; then bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/security-review.sh\" <<< \"$INPUT\"; fi'"
          }
        ]
      }
    ]
  }
}
