{
  "name": "chaos",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/extension",
    "test": "npm run test --workspace=packages/extension",
    "dev": "npm run dev --workspace=packages/extension"
  }
}
