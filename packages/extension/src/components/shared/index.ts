/**
 * CHAOS Shared Components — barrel export.
 *
 * Importing this file registers shared custom elements (`chaos-sidebar`, `chaos-filter-bar`).
 */
export { ChaosSidebar } from './sidebar.js';
export { ChaosFilterBar } from './filter-bar.js';
export type { FilterConfig } from './filter-bar.js';
